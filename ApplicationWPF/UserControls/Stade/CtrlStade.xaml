<UserControl x:Class="ApplicationWPF.UserControls.CtrlStade"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DesignHeight="100" d:DesignWidth="500"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" Content="Planete"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Planete, UpdateSourceTrigger=PropertyChanged}" />
        
        <Label Grid.Row="1" Grid.Column="0" Content="Nombre de place"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NbPlaces, UpdateSourceTrigger=PropertyChanged}"/>
        
        <Label Grid.Row="2" Grid.Column="0" Content="Caracteristiques"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Caracteristiques, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
